# @package _global_
defaults:
  - _self_
  - paths: local
  - completion: llm
  - override hydra/launcher: joblib

gitranking_path: ${raw_data}/gitranking.jsonl
redo: False

hydra:
  launcher:
    n_jobs: 8